# 2026-05-08

## SketchTool-Tuari UI 修改
- 移除轮播底部控制栏的页码指示器（playbackPageIndicator），让控制按钮更简洁
- 移除"继续上次"按钮（resumeLastProgressButton），起始页码输入框直接默认显示上次中断的页码（逻辑已在 loadImagesForSketchFolder 中通过 findProgressIndex 实现）
- 清理了对应 HTML、CSS（#playbackPageIndicator、.compact-action）、dom.js 和 app-controller.js 中的引用
- 随机模式下 syncStartPageInput 不修改输入框数字；从随机切到顺序时先从输入框读取当前值同步回 startImageIndex，避免覆盖
- 合并倒计时设置：把"显示/隐藏"和"样式选择"两行合并为一行 radio group（隐藏 | 只秒 | 时:分:秒 | 进度条），选隐藏时 isCountdownHidden=true
- 起始页多语言适配：label 加 data-i18n="startPage"，totalLabel 用 t("startPageTotal")，tooltip 用 t("startPageRandomOnly")，三语翻译已添加

## 构建 & 部署
- 代码提交并推送到 GitHub: `6f42a26` feat: UI polish
- Release 构建成功：`npx tauri build`，产物 `Cosmos.app`（18MB）位于 `src-tauri/target/release/bundle/macos/Cosmos.app`
- GitHub SSH 连接异常（IP 198.18.0.36 非 GitHub 真实 IP，疑似代理/DNS 拦截），但 HTTPS push 正常工作
